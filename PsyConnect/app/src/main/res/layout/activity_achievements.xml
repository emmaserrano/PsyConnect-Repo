<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/fondo_login">

    <!-- CABECERA PREMIUM -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:paddingHorizontal="24dp"
        android:paddingBottom="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/bg_card"
                android:backgroundTint="@color/blanco"
                android:elevation="2dp"
                android:src="@drawable/ic_back"
                android:contentDescription="@string/achievement_keep_going" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/title_achievements"
                android:textStyle="bold"
                android:textSize="28sp"
                android:textColor="@color/turquesa_oscuro"
                android:layout_marginStart="16dp"/>
        </LinearLayout>

        <TextView
            android:id="@+id/tvTotalAchievements"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/loading_achievements"
            android:textSize="16sp"
            android:textColor="@color/gris_texto"
            android:layout_marginTop="8dp"
            android:layout_marginStart="60dp"/>
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- LISTA DE LOGROS -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerAchievements"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="8dp"
            android:clipToPadding="false"
            android:paddingBottom="24dp"
            android:visibility="gone"
            tools:listitem="@layout/item_achievement"/>

        <!-- ESTADO VACÍO (EMPTY STATE) -->
        <LinearLayout
            android:id="@+id/layoutEmptyState"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="32dp"
            android:visibility="gone">

            <com.airbnb.lottie.LottieAnimationView
                android:id="@+id/lottieEmpty"
                android:layout_width="200dp"
                android:layout_height="200dp"
                app:lottie_rawRes="@raw/growth"
                app:lottie_autoPlay="true"
                app:lottie_loop="true"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/empty_achievements_title"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="@color/turquesa_oscuro"
                android:layout_marginTop="16dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/empty_achievements_desc"
                android:textSize="15sp"
                android:textColor="@color/gris_texto"
                android:gravity="center"
                android:layout_marginTop="8dp"/>
        </LinearLayout>

        <!-- CARGANDO -->
        <ProgressBar
            android:id="@+id/progressLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminateTint="@color/turquesa_principal"
            android:visibility="visible"/>

    </FrameLayout>

</LinearLayout>
