<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PsyConnect"
        android:usesCleartextTraffic="true">

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Pantallas -->
        <activity android:name=".ui.screens.Login"/>
        <activity android:name=".ui.screens.Register"/>
        <activity android:name=".ui.screens.Home"/>
        <activity android:name=".ui.screens.Test"/>
        <activity android:name=".ui.screens.Results"/>
        <activity android:name=".ui.screens.History"/>
        <activity android:name=".ui.screens.Profile"/>

    </application>

</manifest>